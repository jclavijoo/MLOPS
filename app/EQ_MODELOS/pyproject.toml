[project]
name = "eq-modelos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jclavijoo", email = "jclavijoo@icloud.com" }
]
requires-python = ">=3.10"
dependencies = [
    "joblib>=1.6.0",
    "numpy>=2.2.6",
    "schedule>=1.2.2",
    "scikit-learn>=1.7.2",
]

[project.scripts]
eq-modelos = "eq_modelos:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
